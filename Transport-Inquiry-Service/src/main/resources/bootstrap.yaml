#spring:
#  cloud:
#    config:
#      enabled: true
#      uri: http://config-server-svc:8888
#  datasource:
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
#server:
#  port: 9005

spring:
  cloud:
    config:
      enabled: true
      uri: "http://localhost:8888"
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
server:
  port: 9005